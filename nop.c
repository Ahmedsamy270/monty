#include "monty.h"

/**
 * nop - is a function that doing nothing
 * @stack: is apointer to the top of the stack
 * @line : is a number of the line in opcode
 */

void nop(stack_t **stack, unsigned int line)
{
	(void)stack;
	(void)line;
}
